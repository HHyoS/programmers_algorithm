#include <string>
#include <vector>
#include <map>

using namespace std;
vector<vector<int>> room;
void ins_bo(int x, int y, map<pair<int,int>,int> gisible, map<pair<int,int>,int> bosible){
    if(gisible[{y,x}]==1 ||  gisible[{y,x+1}]==1 || (bosible[{y,x}]==1 && bosible[{y,x+1}] == 1)){
        
    }
}
void ins_gidoong(int x, int y, map<pair<int,int>,int> gisible, map<pair<int,int>,int> bosible){
    
}
void del_bo(int x, int y, map<pair<int,int>,int> gisible, map<pair<int,int>,int> bosible){
    
}
void del_gidoong(int x, int y, map<pair<int,int>,int> gisible, map<pair<int,int>,int> bosible){
    
}
vector<vector<int>> solution(int n, vector<vector<int>> build_frame) {
    vector<vector<int>> answer;
    map<pair<int,int>,int> gisible;
    map<pair<int,int>,int> bosible;
    amp<pair<int,int>
    for(int a = 0; a < n; ++a)
        gisible[{0,a}] = 1;
    room.assign(n , vector<int>(n, 0));
    for(auto a : build_frame){
    }
    return answer;
}
